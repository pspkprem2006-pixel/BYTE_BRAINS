# ByteBrains frontend environment variables.
# Copy this file to ".env" and adjust values locally. Never commit ".env".

# Base URL of the ByteBrains backend API.
# Used by the frontend in later phases (e.g. fetching health, AI features).
VITE_API_URL=http://localhost:8000